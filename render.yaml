databases:
  - name: darklord-xmd-db
    plan: free
    databaseName: darklord_xmd
    user: darklord_xmd

services:
  - type: web
    name: darklord-xmd
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: SESSION_ID
        sync: false
      - key: MODE
        value: public
      - key: TIME_ZONE
        value: Africa/Nairobi
      - key: AUTO_LIKE_STATUS
        value: "true"
      - key: AUTO_READ_STATUS
        value: "true"
      - key: DATABASE_URL
        fromDatabase:
          name: darklord-xmd-db
          property: connectionString
